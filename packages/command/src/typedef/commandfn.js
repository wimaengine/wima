/** @import { Command } from "../core" */
/** @import {World} from '@wimaengine/ecs' */

/**
 * @template {Command} T
 * @callback CommandFn
 * @param {T} command
 * @param {World} registry
 * @returns {void}
 */
export {}
