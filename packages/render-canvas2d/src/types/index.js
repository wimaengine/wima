import { Mesh, TextureCache } from '@wimaengine/render-core'

/**
 * @template T
 * @typedef {(ctx:CanvasRenderingContext2D,material:T,mesh:Mesh,textures:TextureCache<HTMLImageElement>)=>void} Canvas2DFunction
 */

export default {}
